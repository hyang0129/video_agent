[pytest]
testpaths = tests
python_files = test_*.py
markers =
    integration: integration tests that may require API keys or network access
    fixture_integration: integration tests that use local fixture files and require rhubarb.exe
